# tests/curated/ — reviewed tests

Human-reviewed, corrected versions of tests generated by `specgen`. A test
only appears here after it has passed the critique gate documented in
`evaluation/EVALUATION.md`.

This directory is populated **by hand** during review — never by the
generation pipeline.
